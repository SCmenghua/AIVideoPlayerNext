# Generated Flutter target

Run `flutter create . --platforms=windows,android,ios` from this directory's parent (`app/`) after installing Flutter. This placeholder keeps the target boundary visible before SDK-generated files are created.
